<UserControl x:Class="Mil.Paperwork.WriteOff.Views.ResidualValueReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        
             xmlns:converters="clr-namespace:Mil.Paperwork.Infrastructure.Converters;assembly=Mil.Paperwork.Infrastructure"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Mil.Paperwork.WriteOff.Views"
             xmlns:ViewModels="clr-namespace:Mil.Paperwork.WriteOff.ViewModels.Reports"
             d:DataContext="{d:DesignInstance Type=ViewModels:ResidualValueReportViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Background="PaleGoldenrod">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="ColumnParamTitle" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>


            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                        Orientation="Horizontal">

                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                        HorizontalAlignment="Center"
                        Text="Відомість залишкової вартості" FontSize="20" FontWeight="Bold" Margin="5" />

                <TextBlock Text="Дата:" FontSize="20"/>
                <DatePicker SelectedDate="{Binding WriteOffDate}" Width="100" 
                            HorizontalAlignment="Left" Margin="5"/>

                <TextBlock Text="Рапорт №" FontSize="20"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EventReportNumber}" 
                         Width="50"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="6" Orientation="Horizontal">
                <TextBlock Text="Шлях" />
                <TextBox Text="{Binding DestinationFolderPath}" Width="300" VerticalAlignment="Center" IsReadOnly="True" />
                <Button Grid.Row="0" Grid.Column="2" 
                    Padding="0"
                    Content="..." Command="{Binding SelectFolderCommand}" Width="30" VerticalAlignment="Center" />
            </StackPanel>

        </Grid>

        <Grid Grid.Row="1" Background="Bisque">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock TextAlignment="Left" FontSize="14">
                <Run Text="Ціна 1 кг брухту металу"/>
                <LineBreak/>
                <Run Text="(дорогоцінних металів - 1 граму), грн:"/>
            </TextBlock>

            <ItemsControl Grid.Column="2" ItemsSource="{Binding MetalCostCollection}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,1,0" BorderBrush="LightGray" Padding="6" Width="80">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap"/>
                                <TextBox Text="{Binding Cost, StringFormat=N2}" TextAlignment="Center" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <DataGrid Grid.Row="2" Grid.Column="0" 
                  x:Name="AssetsDataGrid"
                  DataContext="{Binding AssetsTable}"
                  ItemsSource="{Binding AssetsCollection}" 
                  SelectedItem="{Binding SelectedAsset}" 
                  EnableRowVirtualization="False"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Назва" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.ProductsSelector.Products, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                      Text="{Binding Name}"
                                      SelectedValue="{Binding SelectedProductId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="AlmostUniqueID"
                                      IsEditable="True"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Center">
                                <Behaviors:Interaction.Triggers>
                                    <Behaviors:EventTrigger EventName="SelectionChanged">
                                        <Behaviors:InvokeCommandAction Command="{Binding ProductSelectedCommand}" 
                                                                       CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                                    </Behaviors:EventTrigger>
                                </Behaviors:Interaction.Triggers>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="С/Н" Binding="{Binding SerialNumber}" Width="80"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>
                <DataGridTextColumn Header="Код ном." Binding="{Binding NomenclatureCode}" Width="70"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>
                <DataGridTemplateColumn Header="Од. виміру" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.MeasurementUnits, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                      Text="{Binding MeasurementUnit}"
                                      DisplayMemberPath="ShortName"
                                      IsEditable="True"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="К-сть" Binding="{Binding Count}" Width="40"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>
                <DataGridTextColumn Header="Ціна" Binding="{Binding Price}" Width="60"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>
                <DataGridTextColumn Header="Кат-ія" Binding="{Binding Category}" Width="50"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

                <DataGridTemplateColumn Header="Взято на облік" Width="*" MaxWidth="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Language="uk-UA"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Рік" Binding="{Binding YearManufacured}" Width="50"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

                <DataGridTextColumn Header="Ресурс, р." Binding="{Binding ResourceYears}" Width="65"
                                    ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Generate Report" Command="{Binding GenerateReportCommand}"/>
            <Button Content="Add Row" Command="{Binding AssetsTable.AddRowCommand}" />
            <Button Content="Remove Row" Command="{Binding AssetsTable.RemoveRowCommand }" />
            <Button Content="Clear Table" Command="{Binding AssetsTable.ClearTableCommand}"/>
            <Button Content="Закрити" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>